Конечно! Вот **полный, чёткий и структурированный промпт**, который вы можете использовать в другой нейросети для генерации кода **серверного локального приложения с пользовательским интерфейсом для Битрикс24** согласно вашему ТЗ и официальной документации:

---

### 🧠 Промпт для нейросети:

Создай **серверное локальное приложение с пользовательским интерфейсом** для интеграции с **Битрикс24**, соответствующее следующим требованиям:

#### 📌 Общие требования:
- Приложение должно быть **локальным (server-side)**, как описано в официальной документации:  
  https://apidocs.bitrix24.ru/local-integrations/serverside-local-app-with-ui.html  
- Оно должно размещаться на **вашем HTTPS-сервере** и устанавливаться в Битрикс24 через раздел *Разработчикам → Локальное приложение*.
- Используй **официальный CRest SDK** для PHP (или его упрощённую версию) для обработки авторизации и вызовов REST API.
- Приложение должно работать **внутри iframe Битрикс24** и использовать авторизацию текущего пользователя.

---

#### 🏷️ Название и назначение:
- Название: **«Финансовый учет РПК Сталкер»**
- Цель: ведение финансового учёта (доходы, расходы, переводы, плановые платежи, отчёты) с привязкой к сущностям CRM (сделки, контакты, компании).

---

#### 👥 Роли и права:
1. **Администратор (Руководитель)**:
   - Полный доступ ко всем данным.
   - Управление справочниками: счета, статьи доходов/расходов, направления деятельности.
   - Управление пользователями и их правами в приложении.
2. **Менеджер**:
   - Доступ только к операциям по своим сделкам/контактам/компаниям.
   - Может создавать/редактировать операции и устанавливать плановые даты оплаты.
   - Видит только свои данные в отчётах и списках.
   - Может фильтровать по периодам (день, неделя, месяц, год).

> 💡 Определяй роль через права текущего пользователя в Битрикс24 (например, по группе или должности).

---

#### 📚 Справочники (управляются администратором):
1. **Счета**:
   - Типы: «Расчетный счет», «Карта», «Наличные», «Робокасса», «Другое».
   - Валюта: только рубль.
   - Поля: название, тип, начальный баланс, текущий баланс.
2. **Статьи доходов и расходов**:
   - Два отдельных иерархических справочника.
   - Примеры:  
     Доходы — «Выручка», «Взнос собственных средств» и т.д.  
     Расходы — «Закупка материалов», «Зарплата», «Налоги» и т.д.
   - Возможность архивировать статьи.
3. **Направления деятельности** (плоский список):
   - «Наружка», «Внутреннее оформление», «Полиграфия», «Сувениры», «Текстиль», «Печати и штампы», «Услуги».

> ⚠️ Справочники должны храниться **внешне** (в вашей БД или в пользовательских полях Битрикс24), но управляться через интерфейс приложения.

---

#### 💰 Основные операции:
- Типы: **Доход**, **Расход**, **Перевод**.
- Форма операции включает:
  - Дата/время (по умолчанию — сейчас).
  - Тип.
  - Сумма.
  - Счёт(а):  
    - Доход → «Счёт зачисления»  
    - Расход → «Счёт списания»  
    - Перевод → «Счёт списания» + «Счёт зачисления»
  - Статья (из соответствующего справочника).
  - Направление деятельности.
  - Привязка к CRM: выбор сделки, контакта, компании, сотрудника (опционально).
  - Комментарий.
  - **Split Payment**: возможность разнести одну сумму по нескольким статьям/направлениям.

---

#### 📅 Планирование и уведомления:
- Отдельный раздел **«Календарь платежей»** для плановых операций.
- Поддержка **разовых и повторяющихся** плановых операций.
- Менеджеры могут создавать плановые доходы по своим сделкам.
- Плановая операция **не становится фактической автоматически** — требуется ручное подтверждение.
- Просроченные плановые даты:
  - Подсвечиваются **красным**.
  - Можно редактировать (переносить).
- При наступлении даты оплаты по сделке — отправлять **уведомление через стандартный «колокольчик» Битрикс24** ответственному менеджеру.

---

#### 📊 Отчёты и аналитика (все с фильтрами по периоду, счёту, статье, направлению, ответственному):
1. **Движение ДС (Cash Flow)** — начальный остаток, обороты, конечный остаток.
2. **Прибыли и убытки (P&L)** — доходы минус расходы.
3. **Взаиморасчёты с контрагентами** — дебиторская/кредиторская задолженность.
4. **Аналитика по сделкам** — рентабельность (доходы − расходы по сделке).
5. **План/факт анализ** — сравнение плановых и фактических показателей с % выполнения.

> 📈 Все отчёты должны включать **графики/диаграммы** и функцию **экспорта в Excel/CSV**.

---

#### 🧩 Интеграция с интерфейсом Битрикс24:
- Основное приложение — отдельный пункт в **левом меню** портала.
- Во **вкладках карточек Сделки, Контакта, Компании** должна появиться новая вкладка **«Финансы»**.
  - Отображает все операции (фактические и плановые), привязанные к сущности.
  - Показывает итоги: доходы, расходы, баланс.
  - Кнопка **«Добавить операцию»**, которая автоматически привязывает новую операцию к текущей сущности.

---

#### 🎨 Дизайн и цветовая схема:
- **Доходы** → фон **бледно-зелёный** (`#f0fdf4` или аналог).
- **Расходы** → фон **бледно-розовый** (`#fef2f2` или аналог).
- **Акцентный цвет** (кнопки, заголовки, выделение) → **вишнёвый** (`#9d174d` или `#8b003a`).
- Основной текст → **тёмно-серый** (`#1f2937`).
- Интерфейс — современный, адаптивный, с использованием Tailwind CSS или аналога.

---

#### 📁 Требуемая структура проекта:
```
your-domain.com/stalker-finance/
├── index.php                 ← точка входа, обрабатывает POST от Битрикс24, инициализирует CRest
├── sdk/                      ← CRest SDK
├── assets/                   ← CSS, JS, изображения
├── api/                      ← обработчики AJAX-запросов (операции, справочники и т.д.)
├── views/                    ← HTML-шаблоны (или один SPA-файл)
└── config.php                ← настройки подключения к вашей БД (если используется)
```

#### 🔐 Авторизация:
- Используй упрощённый OAuth 2.0 через CRest.
- Сохраняй `member_id` и `auth`-данные в сессию или БД.
- Все вызовы API к Битрикс24 — через `CRest::call()`.

#### 💾 Хранение данных:
- Финансовые операции и справочники **рекомендуется хранить во внешней БД** (MySQL/PostgreSQL), а не в CRM.
- Привязка к CRM — через ID сущностей (DEAL_ID, CONTACT_ID и т.д.).

#### 🧪 Тестирование:
- Перед установкой в Битрикс24 проверь сервер через `checkserver.php` (из CRest SDK).
- Убедись, что сервер поддерживает HTTPS, PHP 7.4+, cURL, JSON.

---

Создай **полный рабочий пример**:
1. `index.php` — обработка запуска из Битрикс24, рендер интерфейса.
2. JavaScript-интерфейс с mock-данными (или с вызовами к `api/`).
3. Примеры вызовов `CRest::call()` для получения пользователя, сделок и т.д.
4. Пример вкладки «Финансы» в карточке сделки (через `PLACEMENT = CRM_DEAL_DETAIL_TAB`).

> ⚠️ Не используй localStorage. Всё состояние — через сервер или сессию.

---

Этот промпт даст другой нейросети чёткое понимание архитектуры, требований и ожидаемого результата. Удачи!
